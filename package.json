{
  "name": "dashboard-ultimate-v6",
  "private": true,
  "version": "6.3.0",
  "type": "module",
  "description": "Ultimate Dashboard Godmode: health, energy, learning, culture and evidence-based execution.",
  "scripts": {
    "check": "node scripts/check.js && node scripts/check-command.js && node scripts/check-v6.js && node scripts/check-godmode.js",
    "check:v6": "node scripts/check-v6.js && node scripts/check-godmode.js"
  },
  "engines": { "node": ">=20" }
}
